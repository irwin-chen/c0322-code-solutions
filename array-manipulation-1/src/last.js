/* exported last */

function last(array) {
  return array[array.length - 1];
}

// last(array)
// takes in an array, returns last element
// -return array @ last index
//   -length property of array, - 1 to account for index starting from count 0;
