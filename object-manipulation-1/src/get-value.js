/* exported getValue */

function getValue(object, key) {
  return object[key];
}

// getValue(object, key)
// enter an object and its key property name, get the key value back
//  - return the key property value from the object using bracket notation
