/* exported setValue */

function setValue(object, key, value) {
  object[key] = value;
}

// setValue(object, key, value)
// assign a new key property/value to an object
// - assign new property/value to an object using bracket notation
