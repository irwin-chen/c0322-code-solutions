/* exported lastChars */

function lastChars(length, string) {
  return string.slice(-length);
}

// lastChars(length,string)
// takes in an argument string and length, resturns the length of the string from the end of the string
// - slice the string from the back, at the length specified.
// - return the result
