/* exported firstChars */

function firstChars(length, string) {
  return string.slice(0, length);
}

// firstChars (length, string)
// takes in argument string and cuts it off at argument length, keeps the portion from the start of the string to specified length
// slice string from beginning to length, return the value
